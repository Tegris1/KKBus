spring:
  application:
    name: kkbus
  datasource:
    url: jdbc:mysql://localhost:3306/kkbus?createDatabaseIfNotExist=true
    username: root
    password: qwerty

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
  jwt:
    secret: ${JWT_SECRET}
    accessTokenExpiration: 900
    refreshTokenExpiration: 604800

app:
  mock-data:
    enabled: false
